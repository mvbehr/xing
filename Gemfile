source "http://rubygems.org"

group :development, :test do
  if RUBY_PLATFORM =~ /darwin/
    # OS X integration
    gem 'terminal-notifier-guard'
    gem "rb-fsevent"
  end
end


# Specify your gem's dependencies in xing.gemspec
gemspec
